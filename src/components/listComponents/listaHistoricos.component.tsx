import React from "react";
import { Button, Text, View, TextInput, FlatList, StyleSheet, ScrollView, Alert } from "react-native";
import { Picker } from "@react-native-picker/picker";

export const ListHistoricosGuard : React.FC = () =>{

    return(
        <View>

        </View>
    )
}